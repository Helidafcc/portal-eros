import { NavLink } from 'react-router-dom'

const link = ({ isActive }) =>
  "px-3 py-2 rounded-xl hover:bg-white/10 " + (isActive ? "bg-white/10 underline" : "")

export default function Shell({ children }) {
  return (
    <div className="min-h-screen text-white"
         style={{background: "radial-gradient(1200px 600px at -10% -10%, rgba(124,58,237,.18), transparent), radial-gradient(1200px 600px at 110% 110%, rgba(59,130,246,.18), transparent), linear-gradient(180deg,#1a1033,#060916)"}}>
      <header className="sticky top-0 z-20 backdrop-blur bg-black/20 border-b border-white/10">
        <nav className="mx-auto max-w-6xl px-4 py-3 flex gap-2">
          <NavLink to="/" className={link}>Início</NavLink>
          <NavLink to="/biblioteca" className={link}>Biblioteca</NavLink>
          <NavLink to="/oraculos" className={link}>Oráculos</NavLink>
          <NavLink to="/tarot" className={link}>Tarot</NavLink>
        </nav>
      </header>

      <main className="mx-auto max-w-6xl px-4 py-8">{children}</main>

      <footer className="mx-auto max-w-6xl px-4 py-10 text-xs opacity-70">
        © {new Date().getFullYear()} Esphera • Visual místico
      </footer>
    </div>
  )
}