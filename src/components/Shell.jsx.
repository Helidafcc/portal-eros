// src/components/Shell.jsx
import React from "react";

export default function Shell({ children }) {
  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-900 via-indigo-900 to-black text-white">
      {/* Topo / Cabeçalho */}
      <header className="p-4 border-b border-purple-700 shadow-lg">
        <h1 className="text-2xl font-bold text-center text-yellow-400">
          🌌 Portal Místico Esphera 🌌
        </h1>
      </header>

      {/* Conteúdo principal */}
      <main className="p-6">{children}</main>

      {/* Rodapé */}
      <footer className="p-4 border-t border-purple-700 text-center text-sm text-gray-400">
        ✨ Conecte-se com o mistério do universo ✨
      </footer>
    </div>
  );
}